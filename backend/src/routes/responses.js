// src/routes/responses.js
const express = require('express');
const router = express.Router();

// Placeholder: Response CRUD routes (students submit, staff review)
router.get('/', (req, res) => {
  res.json({ message: 'Responses endpoint placeholder' });
});

module.exports = router;
